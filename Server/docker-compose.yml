version: '3'
services:
  nodejs_app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./dist/data:/path/to/data
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - nodejs_app
      